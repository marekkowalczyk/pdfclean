# Next Session

- Add LICENSE file (MIT) and --help/--version/--dry-run flags to the bash script (remaining M0 tasks)
- Decide whether to start M1 (multi-backend) in bash first as a prototype, or jump straight to Go rewrite
- Benchmark current cpdfsqueeze results on a set of real PDFs to establish a baseline before adding new backends
